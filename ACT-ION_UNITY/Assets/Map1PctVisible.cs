using Microsoft.Unity.VisualStudio.Editor;
using UnityEngine;

public class Map1PctVisible : MonoBehaviour
{
    private Image myself;
    void Start()
    {
        gameObject.SetActive(true);
    }
}
